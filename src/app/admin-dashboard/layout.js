export const metadata = {
  title: "Admin Dashboard",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function AdminDashboardLayout({ children }) {
  return children;
}
