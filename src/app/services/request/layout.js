export const metadata = {
  title: "Request a Service",
  description: "Submit a consultation request to HAB Global Management.",
  alternates: {
    canonical: "/services/request",
  },
  robots: {
    index: false,
    follow: false,
  },
};

export default function ServiceRequestLayout({ children }) {
  return children;
}
